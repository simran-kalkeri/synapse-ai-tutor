streamlit>=1.35.0
pymupdf>=1.23.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
plotly>=5.18.0
requests>=2.31.0
torch>=2.0.0
numpy>=1.24.0
networkx>=3.0

# ── LLM ──────────────────────────────────────────────────────────────────
groq>=0.9.0                    # Primary LLM provider (Groq cloud API)

# ── Configuration ────────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
structlog>=23.0.0

# ── Database ─────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0                # Async PostgreSQL driver
psycopg2-binary>=2.9.0         # Sync PostgreSQL driver (for Alembic)
alembic>=1.13.0

# ── Authentication ───────────────────────────────────────────────────────
PyJWT>=2.8.0
authlib>=1.3.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
httpx>=0.27.0                  # Async HTTP for OAuth token exchange

# ── RAG / GraphRAG 2.0 ──────────────────────────────────────────────────
rank-bm25>=0.2.2               # BM25 sparse retrieval

# ── Voice Layer ──────────────────────────────────────────────────────────
# STT — Speech-to-Text (local)
faster-whisper>=1.0.0          # PRIMARY STT: fast CPU inference, int8 quantised
openai-whisper>=20231117        # FALLBACK STT: pure Python (no extra ffmpeg needed)
# st.audio_input() handles mic — built into Streamlit >= 1.35, no package needed

# TTS — Text-to-Speech (local / free)
gTTS>=2.3.2                    # PRIMARY TTS: Google TTS, free, no API key
pydub>=0.25.1                  # Audio chunking + MP3 concatenation
imageio-ffmpeg>=0.4.9          # Bundled ffmpeg binary for Windows (no system install needed)
soundfile>=0.12.1              # Audio file I/O (used by Whisper backends)

# ── Optional Cloud Accelerators ──────────────────────────────────────────
# Uncomment to enable, then set the corresponding key in .env
# elevenlabs>=1.0.0            # ElevenLabs premium TTS (ELEVENLABS_ENABLED=true)
