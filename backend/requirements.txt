
# ── FastAPI core ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.0.0

# ── Auth / Security ───────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0   # used directly in auth.py (passlib NOT used — incompatible with bcrypt>=4)
httpx>=0.27.0

# ── Rate limiting (custom in-memory limiter in api/v1/rate_limiter.py) ────────
# slowapi and redis were previously used but removed in favor of a lightweight
# sliding-window limiter. Remove these lines after confirming no external dep:
# slowapi>=0.1.9
# redis>=5.0.0

# ── AI / LLM ─────────────────────────────────────────────────────────────────
groq>=0.9.0
openai>=1.30.0  # used by ai/llm/client.py for NVIDIA NIM (OpenAI-compatible endpoint)

# ── RAG / Embeddings ─────────────────────────────────────────────────────────
pymupdf>=1.23.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
rank-bm25>=0.2.2
numpy>=1.24.0
networkx>=3.0

# ── Pydantic / Settings ───────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=23.0.0

# ── Database (installed but not activated at runtime — PG is opt-in) ──────────
requests>=2.31.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
authlib>=1.3.0

# ── Voice / Audio ─────────────────────────────────────────────────────────────
gTTS>=2.3.2
pydub>=0.25.1
soundfile>=0.12.1
faster-whisper>=1.0.0
imageio-ffmpeg>=0.4.9
